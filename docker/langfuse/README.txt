Langfuse v3 services are defined in ../docker-compose.yml together with PostgreSQL, Temporal, ClickHouse, Redis, and MinIO.
From the repository root: docker compose -f docker/docker-compose.yml up -d
