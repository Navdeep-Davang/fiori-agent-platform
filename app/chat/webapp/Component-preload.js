/*!
 * Development stub — UI5 probes for Component-preload.js before loading split modules.
 * A missing file returns HTML 404 and breaks MIME checks. Replace with `ui5 build` output for production.
 */
sap.ui.define("acp/chat/Component-preload", [], function () {
    "use strict";
});
